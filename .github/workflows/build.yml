name: "Build YASK"

permissions:
  contents: write

env:
  OUT_DIR: "out"
  KERNEL_VERSION: "5.15"
  ANDROID_VERSION: "13"

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
        
jobs:
  build-kernels:
    name: "🐎 Build YASK ${{ matrix.kernel.name }} kernel"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kernel:
          - name: "GKI"
            dir: "common_gki"
            repo_name: "common"
            repo_url: "https://android.googlesource.com/kernel/common"
            branch: "android13-5.15-lts"
            defconfig: "gki_defconfig"
            local_version: "-YASK-GKI"
            output_name: "Image.gki.ksu"
            enable_ksu: true
            extra_patches:
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-gki-fixup.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-sign-builds.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-Don-t-append-and-dirty-to-localversion.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-do-not-ship-full-auto_localversion.patch"
          - name: "CLO"
            dir: "common_clo"
            repo_name: "clo"
            repo_url: "https://git.codelinaro.org/clo/la/kernel/msm-5.15.git"
            branch: "kernel.lnx.5.15.r1-rel"
            defconfig: "gki_defconfig"
            local_version: "-YASK-CLO"
            output_name: "Image.clo.ksu"
            enable_ksu: true
            extra_patches:
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-gki-fixup.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-sign-builds.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-Don-t-append-and-dirty-to-localversion.patch"
              - "https://raw.githubusercontent.com/topnotchfreaks/kernel_patches/main/0001-scripts-do-not-ship-full-auto_localversion.patch"

    env:
      KERNEL_LOC_DIR: ${{ matrix.kernel.dir }}
      KERNEL_NAME: ${{ matrix.kernel.repo_name }}
      KERNEL_REPO: ${{ matrix.kernel.repo_url }}
      KERNEL_BRANCH: ${{ matrix.kernel.branch }}
      DEFCONFIG_NAME: ${{ matrix.kernel.defconfig }}
      LOCAL_VERSION: ${{ matrix.kernel.local_version }}
      OUTPUT_NAME: ${{ matrix.kernel.output_name }}
      WORK_DIR: ${{ github.workspace }}

    steps:
      - name: "✨ Setup workspace"
        run: |
          mkdir -p ${{ env.KERNEL_LOC_DIR }}
          echo "BUILD_TIME=$(date +'%Y-%m-%d')" >> $GITHUB_ENV
          sudo apt-get update -qq
          sudo apt-get install -y repo rsync aria2

      - name: 📐 Setup SWAP 10GB
        uses: pierotofy/set-swap-space@master
        with:
            swap-size-gb: 10

      - name: "🌟 Clone kernel source"
        working-directory: ./${{ env.KERNEL_LOC_DIR }}
        run: |
          git clone --recursive --branch ${{ env.KERNEL_BRANCH }} \
            ${{ env.KERNEL_REPO }} ${{ env.KERNEL_NAME }} --depth=1

      - name: "📥 Initialize AOSP repo and toolchains"
        working-directory: ${{ env.WORK_DIR }}
        run: |
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android13-5.15-lts --depth=1
          repo sync --optimized-fetch --prune -j8
          rm -rf .repo common
          aria2c -x 16 -s 16 -o prebuilts/clang/host/linux-x86/clang-r547379.tar.gz "https://github.com/topnotchfreaks/clang/releases/download/v1.0.0/clang-r547379.tar.gz"
          mkdir -p prebuilts/clang/host/linux-x86/clang-r547379
          tar -xzf prebuilts/clang/host/linux-x86/clang-r547379.tar.gz -C prebuilts/clang/host/linux-x86/clang-r547379
          sed -i -e 's/^BRANCH=.*/BRANCH=android13-5.15/' -e 's/^CLANG_VERSION=.*/CLANG_VERSION=r547379/' ${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}/build.config.constants
          sed -i '/^POST_DEFCONFIG_CMDS="check_defconfig"/d' ${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}/build.config.gki || true

      - name: "🧩 Configure kernel & patches"
        working-directory: ${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}
        run: |
          CONFIG_FILE="arch/arm64/configs/${{ env.DEFCONFIG_NAME }}"
          echo "CONFIG_LOCALVERSION=\"${{ env.LOCAL_VERSION }}-${{ env.BUILD_TIME }}\"" >> "$CONFIG_FILE"
          echo "CONFIG_LOCALVERSION_AUTO=n" >> "$CONFIG_FILE"
          echo '${{ toJSON(matrix.kernel.extra_patches) }}' | jq -r '.[]' | while read p; do
            [ -n "$p" ] && (curl -L -o temp.patch "$p" && patch -p1 < temp.patch || true)
          done

      - name: "🔧 Serial & CPU ID spoof patch"
        working-directory: ./${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}
        run: |
          cat >> init/main.c << 'EOF'
          #include <linux/kallsyms.h>
          #include <linux/string.h>
          static void safe_spoof(char *bootconfig, const char *key, const char *new_val) {
              char *match = strstr(bootconfig, key);
              if (!match) return;
              match += strlen(key);
              while (*match == '=' || *match == ' ') match++;
              if (*match == '"') match++;
              char *end = match;
              while (*end != '\0' && *end != '\n' && *end != ' ' && *end != '"') end++;
              int orig_len = end - match, new_len = strlen(new_val);
              if (new_len <= orig_len) {
                  memcpy(match, new_val, new_len);
                  for (int i = new_len; i < orig_len; i++) match[i] = ' ';
              }
          }
          void apply_serial_spoof(void) {
              extern char *saved_command_line;
              char **p_saved_boot_config = (char **)kallsyms_lookup_name("saved_boot_config");
              if (saved_command_line) {
                  safe_spoof(saved_command_line, "androidboot.serialno", "a7c2e91b");
                  safe_spoof(saved_command_line, "androidboot.cpuid", "0xa7c2e91b");
              }
              if (p_saved_boot_config && *p_saved_boot_config) {
                  safe_spoof(*p_saved_boot_config, "androidboot.serialno", "a7c2e91b");
                  safe_spoof(*p_saved_boot_config, "androidboot.cpuid", "0xa7c2e91b");
              }
          }
          static int __init init_spoof_trigger(void) { apply_serial_spoof(); return 0; }
          late_initcall(init_spoof_trigger);
          EOF

      - name: "🔧 RAM Spoof (+5MB)"
        working-directory: ./${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}
        run: |
          sed -i 's/show_val_kb(m, "MemTotal:       ", i.totalram);/show_val_kb(m, "MemTotal:       ", i.totalram + 1280);/' fs/proc/meminfo.c
          sed -i '/si_meminfo(info);/a \info->totalram += 1280;' kernel/sys.c

      - name: "🔧 Wi-Fi MAC Spoof (02:11:22:33:44:55)"
        working-directory: ./${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}
        run: |
          gawk -i inplace '/int register_netdevice\(struct net_device \*dev\)/ {
              print; getline; print;
              if ($0 ~ /{/) {
                  print "\tif (dev->name && strncmp(dev->name, \"wlan\", 4) == 0) {"
                  print "\t\tconst unsigned char fake_mac[6] = {0x02, 0x11, 0x22, 0x33, 0x44, 0x55};"
                  print "\t\tmemcpy(dev->dev_addr, fake_mac, 6);"
                  print "\t\tmemcpy(dev->perm_addr, fake_mac, 6);"
                  print "\t}"
              }
              next
          } 1' net/core/dev.c

      - name: "🔧 Setup Wild-KSU with SUSFS"
        if: matrix.kernel.enable_ksu == true
        working-directory: ./${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}
        run: |
          set -e
          curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s canary
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android13-5.15
          patch -p1 < susfs4ksu/kernel_patches/50_add_susfs_in_gki-android13-5.15.patch || true
          cp susfs4ksu/kernel_patches/fs/susfs.c fs/susfs.c
          cp susfs4ksu/kernel_patches/include/linux/susfs.h include/linux/susfs.h
          cp -rf susfs4ksu/kernel_patches/KernelSU/kernel/* Wild_KSU/kernel/ || true
          ln -sf ../Wild_KSU drivers/kernelsu
          cat >> arch/arm64/configs/${{ env.DEFCONFIG_NAME }} << 'EOF'
          CONFIG_KSU=y
          CONFIG_KPROBES=y
          CONFIG_OVERLAY_FS=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          EOF

      - name: "🔨 Build kernel"
        working-directory: ${{ env.WORK_DIR }}
        run: |
          LTO=full BUILD_CONFIG=${{ env.KERNEL_LOC_DIR }}/${{ env.KERNEL_NAME }}/build.config.gki.aarch64 build/build.sh -j$(nproc --all)

      - name: "📁 Copy & Upload"
        working-directory: ${{ env.WORK_DIR }}
        run: |
          cp out/dist/Image out/dist/${{ env.OUTPUT_NAME }}
      
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.kernel.name }}-kernel
          path: out/dist/${{ env.OUTPUT_NAME }}

  package-kernels:
    name: "📦 Package Flashable ZIP"
    runs-on: ubuntu-latest
    needs: [build-kernels]
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: ./artifacts/
      - name: "📦 Create AnyKernel3"
        run: |
          git clone --depth=1 https://github.com/topnotchfreaks/AnyKernel3 -b YASK AnyKernel3
          find ./artifacts -name "Image.*" -exec cp {} AnyKernel3/ \;
          cd AnyKernel3 && zip -r9 "../YASK_SPOOF_$(date +'%Y-%m-%d').zip" * -x .git/*
      - uses: actions/upload-artifact@v4
        with:
          name: YASK-Flashable-ZIP
          path: "*.zip"
